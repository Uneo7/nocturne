import { BookOpen, Database, Flag, Megaphone, Rocket, Server } from 'lucide';
import type { IconArtworkSource } from '@nocturne/watercolour';

export const bookArtwork = { icon: BookOpen, name: 'book-open' } satisfies IconArtworkSource;
export const databaseArtwork = { icon: Database, name: 'database' } satisfies IconArtworkSource;
export const flagArtwork = { icon: Flag, name: 'flag' } satisfies IconArtworkSource;
export const megaphoneArtwork = { icon: Megaphone, name: 'megaphone' } satisfies IconArtworkSource;
export const rocketArtwork = { icon: Rocket, name: 'rocket' } satisfies IconArtworkSource;
export const serverArtwork = { icon: Server, name: 'server' } satisfies IconArtworkSource;
